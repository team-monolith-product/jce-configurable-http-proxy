name: Build Dev

on:
  push:
    branches: [develop]

permissions:
  id-token: write
  contents: write

jobs:
  build:
    uses: team-monolith-product/tmn-gh-actions/.github/workflows/build_v2.yml@develop
    with:
      env: dev
      target: develop
      helmRepository: jce-jupyter-hub-helm
      # INF-136 canary: jupyterhub-1 shard 만 fork 이미지 tag 업데이트.
      propertyPath: jupyterhub-1.proxy.chp.image.tag
      updateTargets: |
        {
          "aws": {
            "imageName": "jce-ecr-chp-all-dev",
            "valueFilePath": ["aws-apne2-dev-jupyter.yaml"]
          }
        }
    secrets:
      MACHINE_TOKEN: ${{ secrets.MACHINE_TOKEN }}
